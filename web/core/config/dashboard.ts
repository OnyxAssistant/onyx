import { DashboardConfig } from "@onyx/types";

export const dashboardConfig: DashboardConfig = {
  sidebarNav: [
    {
      title: "Profile",
      href: "/dashboard/profile",
      icon: "user",
    }
  ],
}
